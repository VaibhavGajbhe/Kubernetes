apiVersion : v1
kind	: Pod

metadata:
  name : firefox-dpod
  namespace	: vaibhav-ns
  labels:
    firefox : bhondu
spec:
  containers:
  - image : linuxserver/firefox
    name : firefox-container
    ports :
    - containerPort : 3000