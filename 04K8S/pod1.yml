apiVersion : v1
kind : Pod

metadata :
  name : todo-app-pod
  namespace : vaibhav-ns

spec :
  containers :
  - image : linuxserver/firefox
    name : firefox-container-01
    ports : 
    - containerPort : 3000
